<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Add the Google Fonts link to include Open Sans font -->
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;700&display=swap" rel="stylesheet">

  <!-- Add Bootstrap CSS link -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

  <!-- Add a title for accessibility -->
  <title><%= @entries.present? ? "Globetrotter's Diary - All Available Entries" : "Globetrotter's Diary - No Entries Available" %></title>

  <!-- Custom CSS for centering the container and making it wider -->
  <style>
    body {
      font-family: 'Open Sans', sans-serif;
      background-color: #f9f9f9; /* Light gray */
      color: #333; /* Dark gray */
    }

    .custom-container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .entry-card {
      margin-bottom: 20px;
      border-radius: 10px;
    }

    .entry-image {
      max-height: 200px;
      object-fit: cover;
    }

    .header-title {
      font-size: 36px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .header-subtitle {
      font-size: 18px;
      line-height: 1.6;
    }

    .lead-text {
      font-size: 18px;
    }

    .pagination {
      justify-content: center;
    }
  </style>
</head>
<body>
  <div class="custom-container mt-4">
    <header class="bg-white text-center py-4 rounded-lg">
      <h1 class="display-4 header-title">Globetrotter's Diary</h1>
      <p class="lead header-subtitle">
        Welcome to Globetrotter's Diary, your gateway to exploring the wonders of the world. Embark on thrilling journeys, discover diverse cultures, savor local flavors, and immerse yourself in awe-inspiring landscapes. Join our community of passionate travelers as we share unforgettable experiences from across the globe.
      </p>
    </header>

    <main class="bg-light p-4 rounded-lg">
      <h2 class="mt-5 mb-4">
        <%= @entries.present? ? "All Available Entries" : "No Entries Available" %>
      </h2>

      <!-- New Entry Button -->
      <div class="mt-4 d-flex justify-content-center">
        <%= link_to "New Entry", new_entry_path, class: "btn btn-success btn-lg" %>
      </div>

      <div class="mb-3 d-flex justify-content-between align-items-center mt-4">
        <strong class="mr-3">Sort By:</strong>
        <% search_params = params[:q]&.permit(:place_name_cont, :description_cont, :latitude_eq, :longitude_eq, :date_visited_gteq, :date_visited_lteq, :tag_eq) || {} %>
        <div class="btn-group" role="group" aria-label="Sort Entries">
          <%= link_to "Date Visited", url_for(q: search_params.merge(s: 'date_visited desc')), class: "btn btn-secondary btn-sm" %>
          <%= link_to "Alphabetical Order", url_for(q: search_params.merge(s: 'place_name asc')), class: "btn btn-secondary btn-sm" %>
        </div>
      </div>

      <% if @entries.present? %>
        <p class="mb-3">
          <%= "Showing #{@entry_count} #{'entry'.pluralize(@entry_count)}" %>
        </p>
        <div class="list-group">
          <% @entries.each do |entry| %>
            <!-- Entry Cards -->
            <div class="list-group-item list-group-item-action bg-white rounded-lg shadow-sm entry-card">
              <div class="row align-items-center">
                <div class="col-md-8">
                  <h4 class="mb-0"><strong>Place Name:</strong> <%= entry.place_name %></h4>
                  <p class="mb-1"><strong>Description:</strong> <%= entry.description %></p>
                  <p class="mb-1"><strong>Latitude:</strong> <%= entry.latitude %></p>
                  <p class="mb-1"><strong>Longitude:</strong> <%= entry.longitude %></p>
                  <p class="mb-1"><strong>Date Visited:</strong> <%= entry.date_visited %></p>
                  <p class="mb-1"><strong>Link:</strong> <%= entry.link %></p>
                </div>
                <% if entry.image.attached? %>
                  <div class="col-md-4 text-center mt-3 mt-md-0">
                    <%= image_tag entry.image, class: "img-thumbnail img-fluid entry-image" %>
                  </div>
                <% end %>
                <div class="col-12 mt-3">
                  <div class="d-flex justify-content-center justify-content-md-start entry-actions">
                    <a href="<%= entry_path(entry) %>" class="btn btn-primary btn-sm mr-2">View</a>
                    <%= link_to "Edit", edit_entry_path(entry), class: "btn btn-secondary btn-sm mr-2" %>
                    <%= button_to "Delete", entry, method: :delete, data: { confirm: "Are you sure you want to delete this entry?" }, class: "btn btn-danger btn-sm" %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>

        <div class="mt-5 d-flex justify-content-center">
          <%= will_paginate @entries, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
        </div>
      <% else %>
        <div class="text-center mt-5">
          <p class="lead">No entries found.</p>
        </div>
      <% end %>

    </main>
  </div>

  <!-- Add Bootstrap JS link -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
