<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Add the Google Fonts link to include Helvetica Neue font -->
  <link href="https://fonts.googleapis.com/css?family=Helvetica+Neue&display=swap" rel="stylesheet">

  <!-- Add Bootstrap CSS link -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom CSS style for the dark blue button -->
  <style>
    .btn-grey {
      background-color: #6c757d;
      color: #ffffff;
    }

    /* Custom CSS for pagination numbers (in gray) */
    .pagination > .page-item > a.page-link,
    .pagination > .page-item > span.page-link {
      color: #6c757d; /* Change this to the desired color (gray) */
      background-color: #ffffff; /* Change this to the desired background color (white) */
      border: 1px solid #6c757d; /* Change this to the desired border color (gray) */
    }

    /* Custom CSS for the active pagination number (in solid gray) */
    .pagination > .page-item.active > a.page-link,
    .pagination > .page-item.active > span.page-link {
      color: #ffffff; /* Change this to the desired color for the active link */
      background-color: #6c757d; /* Change this to the desired background color (solid gray) */
      border: 1px solid #6c757d; /* Change this to the desired border color (gray) */
    }

  </style>

  <!-- Add a title for accessibility -->
  <title><%= @entries.present? ? "Globetrotter's Diary - All Available Entries" : "Globetrotter's Diary - No Entries Available" %></title>
</head>
<body>
  <div class="container mt-4">
    <header class="bg-dark text-white text-center py-4 rounded-lg mb-4">
      <h1 class="display-4">Globetrotter's Diary</h1>
      <p class="lead">
        Welcome to Globetrotter's Diary, your gateway to exploring the wonders of the world. Embark on thrilling journeys, discover diverse cultures, savour local flavors, and immerse yourself in awe-inspiring landscapes. Join our community of passionate travelers as we share unforgettable experiences from across the globe.
      </p>
    </header>

    <main class="bg-light p-4 rounded-lg">
      <h2 class="mt-5 mb-4">
        <%= @entries.present? ? "All Available Entries" : "No Entries Available" %>
      </h2>

      <!-- New Entry Button -->
      <% if user_signed_in? %>
        <div class="d-flex justify-content-center mb-4">
          <%= link_to "New Entry", new_entry_path, class: "btn btn-grey btn-lg" %>
        </div>
      <% else %>
        <div class="d-flex justify-content-center mb-4">
          <%= link_to "Login to Create New Entry", new_user_session_path, class: "btn btn-primary btn-lg" %>
        </div>
      <% end %>

      <div class="mb-4 d-flex justify-content-between align-items-center mt-4">
        <strong class="mr-3">Sort By:</strong>
        <% search_params = params[:q]&.permit(:place_name_cont, :description_cont, :latitude_eq, :longitude_eq, :date_visited_gteq, :date_visited_lteq, :tag_eq) || {} %>
        <div class="btn-group" role="group" aria-label="Sort Entries">
          <%= link_to "Date Visited", url_for(q: search_params.merge(s: 'date_visited desc')), class: "btn btn-grey btn-sm" %>
          <%= link_to "Alphabetical Order", url_for(q: search_params.merge(s: 'place_name asc')), class: "btn btn-grey btn-sm" %>
        </div>
      </div>

      <% if @entries.present? %>
        <p class="mb-3">
          <%= "Showing #{@entry_count} #{'entry'.pluralize(@entry_count)}" %>
        </p>
        <div class="list-group">
          <% @entries.each do |entry| %>
            <!-- Entry Cards -->
            <div class="list-group-item list-group-item-action bg-white rounded-lg shadow-sm mb-4">
              <div class="row align-items-center">
                <div class="col-md-8">
                  <h4 class="mb-2" style="font-size: 22px;"><strong>Place Name:</strong></h4>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.place_name %></p>

                  <h5 class="mb-2" style="font-size: 20px;"><strong>Description:</strong></h5>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.description %></p>

                  <h5 class="mb-2" style="font-size: 20px;"><strong>Latitude:</strong></h5>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.latitude %></p>

                  <h5 class="mb-2" style="font-size: 20px;"><strong>Longitude:</strong></h5>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.longitude %></p>

                  <h5 class="mb-2" style="font-size: 20px;"><strong>Date Visited:</strong></h5>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.date_visited %></p>

                  <h5 class="mb-2" style="font-size: 20px;"><strong>Link:</strong></h5>
                  <p class="mb-4" style="font-size: 18px;"><%= entry.link %></p>
                </div>
                <% if entry.image.attached? %>
                  <div class="col-md-4 text-center mt-3 mt-md-0">
                    <%= image_tag entry.image, class: "img-thumbnail img-fluid rounded-lg", alt: "Image of #{entry.place_name}" %>
                  </div>
                <% end %>
                <div class="col-12 mt-3">
                  <div class="d-flex justify-content-center justify-content-md-start">
                    <a href="<%= entry_path(entry) %>" class="btn btn-grey btn-sm mr-2">View</a>
                    <% if user_signed_in? && current_user == entry.user %>
                      <%= link_to "Edit", edit_entry_path(entry), class: "btn btn-grey btn-sm mr-2" %>
                      <%= button_to "Delete", entry, method: :delete, data: { confirm: "Are you sure you want to delete this entry?" }, class: "btn btn-grey btn-sm" %>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>

        <div class="mt-5 d-flex justify-content-center">
          <%= will_paginate @entries, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
        </div>
      <% else %>
        <div class="text-center mt-5">
          <p class="lead">No entries found.</p>
        </div>
      <% end %>

    </main>
  </div>

  <!-- Add Bootstrap JS link -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
